query($name: String, $route: String, $order: MenuOrderBy, $headingLevel: Int ) {
  menu(name: $name, pathname: $route, orderBy: $order, headingLevel: $headingLevel) {
    item {
      label,
      link
    }
    children {
      item {
        label,
        link
      },
      children {
        item {
          label,
          link
        }
      }
    }
  }
}