query($menu: String!, $route: String) {
  menu(filter: $menu, pathname: $route) {
    item {
      label,
      link
    }
    children {
      item {
        label,
        link
      },
      children {
        item {
          label,
          link
        }
      }
    }
  }
}