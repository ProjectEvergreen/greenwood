query($parent: String!) {
  children(parent: $parent) {
    id,
    label,
    title,
    workspacePath,
    outputPath,
    route,
    layout,
    tableOfContents {
      label,
      route
    }
  }
}