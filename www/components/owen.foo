import './grant.foo';

const node = document.createElement('h6');
const textnode = document.createTextNode('hello from my-custom-file.foo 👋');

node.appendChild(textnode);
document.getElementsByTagName('body')[0].appendChild(node);

console.log('hello from owen.foo! 👋');